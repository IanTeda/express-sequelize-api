/**
 * API resources/endpoints enum
 *
 * @module configs/resources
 */

const resources = {
  CONFIRM_EMAIL_TOKENS: '/confirm-email-tokens',
  RESET_TOKENS: '/reset-tokens',
  THINGS: '/things',
  USERS: '/users',
  AUTHORIZATIONS: '/authorizations',
  ROOTS: '/'
}

export default resources;
